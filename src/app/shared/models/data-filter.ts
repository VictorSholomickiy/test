export interface DataFilter {
  volumeMin: number | null,
  volumeMax: number | null,
  priceRangeMin: number | null,
  priceRangeMax: number | null,
  priceChangePercentageMin: number | null,
  priceChangePercentageMax: number | null,
}
